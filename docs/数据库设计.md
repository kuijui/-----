# 数据库设计文档

## 1. 数据库选型

- **关系型数据库**：MySQL 8.0
- **缓存数据库**：Redis 7.0
- **字符集**：utf8mb4（支持emoji）
- **存储引擎**：InnoDB

## 2. 表结构设计

### 2.1 用户表 (users)

存储用户基本信息

```sql
CREATE TABLE `users` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `openid` VARCHAR(64) NOT NULL COMMENT '微信openid',
  `unionid` VARCHAR(64) DEFAULT NULL COMMENT '微信unionid',
  `nickname` VARCHAR(100) DEFAULT NULL COMMENT '用户昵称',
  `avatar` VARCHAR(500) DEFAULT NULL COMMENT '用户头像URL',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `gender` TINYINT DEFAULT 0 COMMENT '性别：0未知 1男 2女',
  `country` VARCHAR(50) DEFAULT NULL COMMENT '国家',
  `province` VARCHAR(50) DEFAULT NULL COMMENT '省份',
  `city` VARCHAR(50) DEFAULT NULL COMMENT '城市',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1正常 2禁用',
  `daily_free_count` INT DEFAULT 3 COMMENT '每日免费次数',
  `total_generated` INT DEFAULT 0 COMMENT '累计生成次数',
  `last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**字段说明**：
- `openid`：微信唯一标识，用于登录
- `daily_free_count`：每日免费次数，默认3次
- `total_generated`：累计生成次数，用于统计
- `status`：用户状态，可用于封禁违规用户

### 2.2 会员表 (members)

存储用户会员信息

```sql
CREATE TABLE `members` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '会员ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `member_type` VARCHAR(20) NOT NULL COMMENT '会员类型：free免费 monthly月度 quarterly季度 yearly年度',
  `daily_limit` INT DEFAULT 3 COMMENT '每日生成次数限制',
  `expire_at` DATETIME DEFAULT NULL COMMENT '会员过期时间',
  `auto_renew` TINYINT DEFAULT 0 COMMENT '是否自动续费：0否 1是',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1正常 2已过期 3已取消',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_expire_at` (`expire_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会员表';
```

**会员类型配置**：
| member_type | daily_limit | 价格 |
|-------------|-------------|------|
| free | 3 | ¥0 |
| monthly | 30 | ¥19.9 |
| quarterly | 50 | ¥49.9 |
| yearly | 100 | ¥159.9 |

### 2.3 文案生成记录表 (contents)

存储每次生成的文案内容

```sql
CREATE TABLE `contents` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `topic` VARCHAR(200) NOT NULL COMMENT '主题/关键词',
  `description` TEXT DEFAULT NULL COMMENT '内容描述',
  `style` VARCHAR(50) NOT NULL COMMENT '文案风格',
  `length` VARCHAR(20) NOT NULL COMMENT '文案长度：short中 medium长',
  `titles` JSON NOT NULL COMMENT '生成的标题（数组）',
  `content` TEXT NOT NULL COMMENT '正文内容',
  `tags` JSON DEFAULT NULL COMMENT '话题标签（数组）',
  `score` INT DEFAULT 0 COMMENT '爆款指数评分',
  `is_collected` TINYINT DEFAULT 0 COMMENT '是否收藏：0否 1是',
  `ai_model` VARCHAR(50) DEFAULT NULL COMMENT '使用的AI模型',
  `tokens_used` INT DEFAULT 0 COMMENT '消耗的tokens数',
  `generation_time` INT DEFAULT 0 COMMENT '生成耗时（毫秒）',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_created_at` (`created_at`),
  KEY `idx_is_collected` (`is_collected`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文案生成记录表';
```

**JSON字段示例**：
```json
{
  "titles": [
    "🌸春天这样穿，回头率200%！",
    "姐妹们！这几套春季穿搭绝了！",
    "春日氛围感穿搭｜温柔又高级"
  ],
  "tags": ["#春季穿搭", "#OOTD", "#穿搭灵感", "#时尚博主"]
}
```

### 2.4 订单表 (orders)

存储支付订单信息

```sql
CREATE TABLE `orders` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '订单ID',
  `order_no` VARCHAR(64) NOT NULL COMMENT '订单号',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `product_type` VARCHAR(50) NOT NULL COMMENT '产品类型：monthly_member季度_member yearly_member single_use',
  `product_name` VARCHAR(100) NOT NULL COMMENT '产品名称',
  `amount` DECIMAL(10,2) NOT NULL COMMENT '订单金额',
  `pay_amount` DECIMAL(10,2) DEFAULT 0 COMMENT '实付金额',
  `discount_amount` DECIMAL(10,2) DEFAULT 0 COMMENT '优惠金额',
  `payment_method` VARCHAR(20) DEFAULT 'wechat' COMMENT '支付方式',
  `transaction_id` VARCHAR(64) DEFAULT NULL COMMENT '微信支付交易号',
  `status` TINYINT DEFAULT 0 COMMENT '订单状态：0待支付 1已支付 2已取消 3已退款',
  `paid_at` DATETIME DEFAULT NULL COMMENT '支付时间',
  `cancel_at` DATETIME DEFAULT NULL COMMENT '取消时间',
  `refund_at` DATETIME DEFAULT NULL COMMENT '退款时间',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

**订单号生成规则**：
```
ORDER + YYYYMMDD + 随机8位数字
例：ORDER20260327AB123456
```

### 2.5 收藏表 (collections)

存储用户收藏的文案

```sql
CREATE TABLE `collections` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '收藏ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `content_id` BIGINT UNSIGNED NOT NULL COMMENT '文案ID',
  `folder` VARCHAR(50) DEFAULT 'default' COMMENT '收藏夹分类',
  `note` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_content` (`user_id`, `content_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收藏表';
```

### 2.6 使用日志表 (usage_logs)

记录用户每次使用情况，用于统计分析

```sql
CREATE TABLE `usage_logs` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `action` VARCHAR(50) NOT NULL COMMENT '操作类型：generate生成 copy复制 collect收藏 share分享',
  `content_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联文案ID',
  `params` JSON DEFAULT NULL COMMENT '操作参数',
  `ip` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` VARCHAR(500) DEFAULT NULL COMMENT '用户代理',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_action` (`action`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='使用日志表';
```

### 2.7 邀请记录表 (invitations)

记录用户邀请关系

```sql
CREATE TABLE `invitations` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '邀请ID',
  `inviter_id` BIGINT UNSIGNED NOT NULL COMMENT '邀请人ID',
  `invitee_id` BIGINT UNSIGNED NOT NULL COMMENT '被邀请人ID',
  `reward_type` VARCHAR(20) DEFAULT 'free_count' COMMENT '奖励类型：free_count免费次数 commission佣金',
  `reward_value` INT DEFAULT 0 COMMENT '奖励值',
  `status` TINYINT DEFAULT 0 COMMENT '状态：0待发放 1已发放',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `rewarded_at` DATETIME DEFAULT NULL COMMENT '奖励发放时间',
  PRIMARY KEY (`id`),
  KEY `idx_inviter_id` (`inviter_id`),
  KEY `idx_invitee_id` (`invitee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='邀请记录表';
```

### 2.8 系统配置表 (system_configs)

存储系统配置参数

```sql
CREATE TABLE `system_configs` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '配置ID',
  `config_key` VARCHAR(100) NOT NULL COMMENT '配置键',
  `config_value` TEXT NOT NULL COMMENT '配置值',
  `description` VARCHAR(500) DEFAULT NULL COMMENT '配置说明',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';
```

**配置示例**：
```sql
INSERT INTO system_configs (config_key, config_value, description) VALUES
('daily_free_count', '3', '每日免费生成次数'),
('invite_reward_count', '5', '邀请好友奖励次数'),
('ai_model_default', 'gpt-3.5-turbo', '默认AI模型'),
('content_max_length', '500', '文案最大长度');
```

## 3. Redis缓存设计

### 3.1 缓存键命名规范

```
{业务模块}:{数据类型}:{唯一标识}
```

### 3.2 缓存数据结构

#### 3.2.1 用户Token
```
Key: auth:token:{userId}
Type: String
Value: JWT Token
TTL: 7天
```

#### 3.2.2 用户信息缓存
```
Key: user:info:{userId}
Type: Hash
Fields: {
  nickname: "用户昵称",
  avatar: "头像URL",
  memberType: "monthly",
  dailyLimit: 30
}
TTL: 1小时
```

#### 3.2.3 每日使用次数计数
```
Key: usage:daily:{userId}:{date}
Type: String (计数器)
Value: 已使用次数
TTL: 24小时（当天结束）
```

#### 3.2.4 限流计数器
```
Key: ratelimit:ip:{ip}:{minute}
Type: String
Value: 请求次数
TTL: 60秒

Key: ratelimit:user:{userId}:{second}
Type: String
Value: 请求次数
TTL: 1秒
```

#### 3.2.5 热门主题缓存
```
Key: hot:topics
Type: List
Value: ["主题1", "主题2", "主题3"]
TTL: 6小时
```

#### 3.2.6 会员状态缓存
```
Key: member:status:{userId}
Type: Hash
Fields: {
  memberType: "monthly",
  expireAt: "2026-04-27",
  dailyLimit: 30
}
TTL: 30分钟
```

## 4. 索引设计

### 4.1 索引策略

**原则**：
- 高频查询字段建立索引
- 联合索引遵循最左前缀原则
- 避免过多索引影响写入性能

### 4.2 关键索引

#### users表
```sql
-- 主键索引
PRIMARY KEY (id)

-- 唯一索引
UNIQUE KEY uk_openid (openid)

-- 普通索引
KEY idx_created_at (created_at)
KEY idx_status (status)
```

#### contents表
```sql
-- 主键索引
PRIMARY KEY (id)

-- 普通索引
KEY idx_user_id (user_id)
KEY idx_created_at (created_at)
KEY idx_is_collected (is_collected)

-- 联合索引（用户+时间查询）
KEY idx_user_created (user_id, created_at)
```

#### orders表
```sql
-- 主键索引
PRIMARY KEY (id)

-- 唯一索引
UNIQUE KEY uk_order_no (order_no)

-- 普通索引
KEY idx_user_id (user_id)
KEY idx_status (status)
KEY idx_created_at (created_at)

-- 联合索引（用户订单查询）
KEY idx_user_status (user_id, status)
```

## 5. 数据字典

### 5.1 枚举值定义

#### 用户状态 (users.status)
| 值 | 说明 |
|----|------|
| 1 | 正常 |
| 2 | 禁用 |

#### 会员类型 (members.member_type)
| 值 | 说明 | 每日限制 |
|----|------|---------|
| free | 免费用户 | 3次 |
| monthly | 月度会员 | 30次 |
| quarterly | 季度会员 | 50次 |
| yearly | 年度会员 | 100次 |

#### 文案风格 (contents.style)
| 值 | 说明 |
|----|------|
| 种草推荐 | 产品推荐类 |
| 干货分享 | 知识分享类 |
| 好物测评 | 测评类 |
| 生活日常 | 日常记录类 |
| 情感共鸣 | 情感类 |
| 搞笑幽默 | 娱乐类 |

#### 文案长度 (contents.length)
| 值 | 说明 | 字数范围 |
|----|------|---------|
| short | 短文案 | 50-100字 |
| medium | 中文案 | 100-200字 |
| long | 长文案 | 200-400字 |

#### 订单状态 (orders.status)
| 值 | 说明 |
|----|------|
| 0 | 待支付 |
| 1 | 已支付 |
| 2 | 已取消 |
| 3 | 已退款 |

#### 操作类型 (usage_logs.action)
| 值 | 说明 |
|----|------|
| generate | 生成文案 |
| copy | 复制文案 |
| collect | 收藏文案 |
| share | 分享文案 |
| pay | 支付 |

## 6. 数据库优化建议

### 6.1 分表策略

**当数据量达到一定规模时考虑分表**：

#### contents表分表（按月）
```sql
contents_202603
contents_202604
...
```

#### usage_logs表分表（按月）
```sql
usage_logs_202603
usage_logs_202604
...
```

### 6.2 归档策略

**历史数据归档**：
- 3个月前的usage_logs归档到历史表
- 1年前的contents可选择性归档

### 6.3 读写分离

**当QPS较高时**：
- 主库：写操作
- 从库：读操作
- 使用中间件（如ProxySQL）实现自动路由

### 6.4 慢查询优化

**监控慢查询**：
```sql
-- 开启慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;

-- 分析慢查询
EXPLAIN SELECT * FROM contents WHERE user_id = 123 ORDER BY created_at DESC LIMIT 10;
```

## 7. 数据备份方案

### 7.1 备份策略

**全量备份**：
- 频率：每天凌晨3点
- 保留：最近7天

**增量备份**：
- 频率：每小时
- 保留：最近24小时

**binlog备份**：
- 实时备份
- 保留：最近7天

### 7.2 备份脚本示例

```bash
#!/bin/bash
# MySQL全量备份脚本

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/data/backup/mysql"
DB_NAME="ai_copywriter"

mysqldump -u root -p${MYSQL_PASSWORD} \
  --single-transaction \
  --routines \
  --triggers \
  --events \
  ${DB_NAME} | gzip > ${BACKUP_DIR}/${DB_NAME}_${DATE}.sql.gz

# 删除7天前的备份
find ${BACKUP_DIR} -name "*.sql.gz" -mtime +7 -delete
```

## 8. 初始化SQL

### 8.1 创建数据库

```sql
CREATE DATABASE IF NOT EXISTS ai_copywriter 
  DEFAULT CHARACTER SET utf8mb4 
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE ai_copywriter;
```

### 8.2 初始化数据

```sql
-- 插入系统配置
INSERT INTO system_configs (config_key, config_value, description) VALUES
('daily_free_count', '3', '每日免费生成次数'),
('monthly_daily_limit', '30', '月度会员每日限制'),
('quarterly_daily_limit', '50', '季度会员每日限制'),
('yearly_daily_limit', '100', '年度会员每日限制'),
('invite_reward_count', '5', '邀请好友奖励次数'),
('share_reward_count', '2', '分享奖励次数'),
('ai_model_default', 'gpt-3.5-turbo', '默认AI模型'),
('ai_temperature', '0.8', 'AI创意度参数'),
('ai_max_tokens', '800', 'AI最大输出tokens'),
('content_audit_enabled', '1', '是否开启内容审核');
```

## 9. 数据安全

### 9.1 敏感数据加密

**需要加密的字段**：
- users.phone（手机号）
- users.openid（可选）

**加密方式**：
- AES-256加密
- 密钥存储在环境变量中

### 9.2 SQL注入防护

**使用参数化查询**：
```javascript
// ❌ 错误示例
const sql = `SELECT * FROM users WHERE id = ${userId}`;

// ✅ 正确示例
const sql = 'SELECT * FROM users WHERE id = ?';
db.query(sql, [userId]);
```

### 9.3 权限控制

**数据库用户权限**：
```sql
-- 应用程序用户（只有必要权限）
CREATE USER 'app_user'@'%' IDENTIFIED BY 'strong_password';
GRANT SELECT, INSERT, UPDATE ON ai_copywriter.* TO 'app_user'@'%';

-- 禁止DELETE权限（防止误删）
-- 禁止DROP权限（防止误删表）
```

---

**文档版本**：V1.0  
**创建日期**：2026-03-27  
**负责人**：数据库架构师  
**审核状态**：待审核
